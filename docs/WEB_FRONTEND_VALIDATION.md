# 웹 프론트 검증

2026-09-14, Windows / Node.js 20.20.2 / JDK 21 / PostgreSQL 16.15에서 검증했습니다.
백엔드의 DB는 일회용 `pagetuner_test`, 브라우저 데이터는 자체 작성 샘플 15개를 사용했습니다.

## 구현 경계

- `web/`: 독립 React/TypeScript 빌드, 한국어 서재·연결·페이지 리더·기기 보관함.
- `contracts/`: 앱·서버 fixture와 OpenAPI. 웹 타입은 계약에서 생성하며 CI에서 차이를 검사.
- `server/`: 사용자별 번역 목록과 본문 조회, 목록 인덱스, 선택적 웹 정적 파일 패키징.
- `core-*/`, `app/`: 스텝 1의 저장·조회 공통화와 앱 연동을 유지.

## 자동 검증

`web`의 `npm run verify`에서 생성 타입 일치, TypeScript 검사, Vite 프로덕션 빌드와
Vitest 테스트 **44개 통과, 실패·건너뜀 0개**. API 인증·timeout·응답 무결성, 계정별 IndexedDB 저장,
손상 항목 격리·원자 복구·동시 변경 충돌, service worker 설치 상태를 검사합니다.

서버 `:server:test` **18개 통과, 실패·건너뜀 0개**. 실제 PostgreSQL에서 Flyway,
저장 왕복, 동시 저장, 사용자 격리와 새 목록의 페이징·정렬·기존 불완전 행 제외를 검증했습니다.
JSON wire 직렬화와 공개 정적 경로/API 인증 분리 검사도 포함합니다.

`npm run build` 후 `-PbuildTarget=server -PwebDistDir=web/dist :server:bootJar`로
웹을 포함한 서버 JAR을 생성하고 `http://127.0.0.1:8080/`에서 검증했습니다.

## 실제 브라우저 검증

- 잘못된 비밀번호는 폼 오류를 표시하며 정상 로그인은 사용자 소유 15개 번역본을 조회.
- 화면별 목록 페이지 이동과 서버의 12개 묶음 경계를 넘어 마지막 15/15 항목 도달.
- 1280×720 데스크톱과 390×844 모바일의 서재·리더 확인. 모바일 본문과 고정 버튼 넘침 없음.
- 방향키로 페이지 이동, 본문 보관, 로컬 계정 전환 시 다른 계정의 책이 표시되지 않음을 확인.
- 서버 프로세스를 실제 종료한 후 페이지 새로고침 성공. 서버 인증은 초기화되고 기기 보관함에서
  저장본을 다시 열어 같은 문단 `preview-p-2`, 문자 위치 59를 복원.
- 모바일 기본 글자 크기의 전체 7페이지를 실제 DOM에서 순서대로 수집. 9개 문단이 연속된
  문자 범위를 유지하고, 합친 본문의 SHA-256이 원본 `payloadHash`와 일치. 모든 페이지가 읽기 영역 안에 표시.
- 마지막 문단에서 글자 크기를 20→32→20px로 각각 6회 변경해도 `preview-p-9`, 문자 위치 0을 유지.
  확대 시 17/18, 원복 시 7/7 페이지로 복원되며 본문 넘침 없음. 화면 재계산과 명시적인 페이지 이동을 분리.

검증 중 발견한 손상 레코드 전체 목록 실패, IndexedDB 열기 재시도 실패, 설치 전 오프라인 완료 표시,
모바일 버튼 접근성 이름 누락, 삭제창 포커스 복원, 반복 글자 크기 변경 시 위치가 밀리는 문제를 수정했습니다.

공개 서비스 배포, 실제 E-Ink 하드웨어/모바일 Safari, 외부 번역 제공자와 Drive 실호출은
이번 웹 검증에 포함하지 않았습니다. 오프라인 기기 사본은 브라우저 프로필 단위이며 암호화된 계정 저장소가 아닙니다.

## 웹소설 불러오기·번역 실행 확장

2026-09-14 확장 구현 후 `web`에서 `npm run verify`를 실행했습니다.
두 OpenAPI 계약의 생성 타입 일치, TypeScript 검사, Vite 프로덕션 빌드와
Vitest **55개 통과, 실패·건너뜀 0개**를 확인했습니다.

- [번역 저장 계약](../contracts/translation-v1.openapi.json)은 optional `bookTitle`·`chapterTitle`을
  저장 요청·상세·요약에 포함합니다. 기존 fixture와 artifact/revision/payload 해시는 그대로 유지합니다.
- [워크플로 계약](../contracts/workflow-v1.openapi.json)은 소스·검색·목차, 원문 import/detail/summary,
  번역기 목록, 번역 작업 생성·목록·상태·취소를 정의합니다. 수동 DTO는 생성된 타입의 alias로 교체했습니다.
- 원문 목록은 `ChapterSummary`만 반환하며, 회차를 열 때 `StoredChapter`를 조회합니다.
  본문이 없는 요약 응답을 오류로 처리하던 불일치를 수정하고 회귀 테스트를 추가했습니다.
- 쓰기 요청의 CSRF 선행 조회, CSRF 실패 시 POST 차단, 같은 서버로만 보내는 인증,
  계정 종료 중 요청 중단, 실패한 mutation 자동 재전송 금지를 검사했습니다.
- 재시도 설정은 서버가 제공한 언어·endpoint·model·glossary를 보존합니다. 자격증명은 응답에서
  투영하지 않으며 API 키는 재입력합니다. 불가능한 진행 건수와 완료 결과 누락은 읽기 전에 거부합니다.
- 원문과 번역문은 서로 다른 `ReadingDocument.kind`를 사용하고 공통 페이지 측정·anchor 로직을 공유합니다.

이 절의 결과는 자동 검증 범위입니다. 위의 실제 브라우저 검증 목록은 기존 번역 서재에 대한 기록이며,
새로운 외부 소스·번역 실행의 브라우저 검증은 서버 연동 검증과 함께 별도로 기록합니다.

## 회원가입·계정 언어 설정 확장

계정 생성·프로필 조회·설정 변경의 [공통 계약](../contracts/accounts-v1.openapi.json)과
생성 타입을 추가했습니다. `npm run check:api`가 저장·워크플로·계정의 세 계약을 확인합니다.

가입은 작은 화면을 고려해 계정 정보와 언어 선택 두 단계로 나눴습니다. 가입 후 로그인하며,
프로필에서 UI 언어와 기본 번역 언어를 별도로 변경합니다. 기존 주요 화면의 한국어 문구를
중앙 카탈로그로 추출하고 영어 팩을 연결했습니다. 미지원 BCP 47 선호는 영어로 대체 표시하고,
언어팩 등록 함수를 통해 추가 언어를 연결할 수 있습니다.

추가 자동 검증 8개는 가입 이름 정규화, 비밀번호 공백 보존·UTF-8 길이·제어문자 검사,
가입의 공개 CSRF와 무인증 요청, 프로필 PATCH, 요청 중 계정 종료, 언어 지역·미지원 fallback,
추후 언어팩 등록과 텍스트 보간을 검사합니다. 계정 비밀번호는 응답 투영과 언어팩에서 제외합니다.

확장 후 `npm run verify`에서 세 계약 검사·타입 검사·프로덕션 빌드와 전체 테스트
**63개 통과, 실패·건너뜀 0개**를 확인했습니다.

## 원문 오프라인·개인 서재·로컬 문서 번역 확장

2026-09-14 23:19 KST `npm run verify`에서 세 계약 타입 일치, TypeScript 검사,
프로덕션 빌드 및 **90개 테스트 통과, 실패·건너뜀 0개**를 확인했습니다.
워크플로 OpenAPI의 독립 스키마 검증도 통과했습니다. 이후 추가되는 묶음 작업은 이 결과에 포함하지 않습니다.

- 원문은 문단 ID·순서·본문으로 서버와 동일한 source revision을 계산한 후 계정별 IndexedDB에 보관합니다.
  메타데이터 digest도 검사하며 손상된 항목은 정상 목록에서 격리하고 삭제할 수 있습니다.
- 책별 용어집의 200항목 제한·대소문자 중복·JSON 크기 한도·import/export를 검증했습니다.
  glossary fingerprint fixture가 서버 알고리즘과 일치하며 다음 번역 요청에 용어집을 포함합니다.
- 용어집 확장의 인물/장소/일반 용어·활성/대소문자·표시 별칭을 단위 검증했습니다.
  기본 옵션은 기존 source/target JSON과 fingerprint를 유지합니다. 앱 v1 JSON 파일의 옵션을
  보존하고 앱에서 잘리는 160자 초과 용어는 내보내기 전에 알립니다. 읽기 표시 별칭의 길이가
  달라져도 원본 좌표를 저장하며, 별칭 일부 선택은 원본 용어 범위로 확장합니다.
  화면 이동·실제 앱 파일 왕복의 검증 결과와 구분합니다.
- 즐겨찾기와 소스 URL을 분리해 저장하고 계정 간 조회·삭제 격리, URL 내 자격증명 거부를 검사했습니다.
- SourceFilters 생성 계약과 서버가 광고한 필터 값을 그대로 보내는 catalog 요청을 검사했습니다.
- 로컬 TXT·Markdown·EPUB·PDF의 읽기·무결성·문단·읽기 메모 테스트가 포함됐습니다.
  PDF의 실제 파서와 페이지 렌더링 테스트는 별도 에이전트가 추가했습니다.
- 로컬 문서 번역 입력은 파일 자산과 기기 메모를 제외한 명시적 텍스트 투영입니다.
  10,000문단·100만자 한도, CSRF를 동반한 업로드와 URL이 빈 uploaded-document 응답을 검사했습니다.
- 영어 팩에 개인 서재·용어집·문서·PDF 오류 메시지를 추가했습니다. 개발 StrictMode의 effect 재실행에서도
  개인 저장소 인스턴스를 잘못 닫지 않도록 처리하고 온라인·오프라인 원문 읽기 위치 키를 공유합니다.

프로덕션 공개 오프라인 자산은 PDF 폰트·CMap·wasm·worker를 포함한 59개입니다.
PDF 모듈을 포함한 JS chunk 크기 및 정적·동적 import 혼합 경고가 있으며 빌드 오류는 아닙니다.
새 UI의 실제 모바일 레이아웃·서버 연결·오프라인 재방문 검증은 위 자동 테스트와 별도로 진행합니다.

## 묶음 회차·독서 환경 확장 통합

2026-09-14 23:26 KST `npm run verify`에서 **109개 테스트·16개 파일 통과**,
세 계약 생성 일치·TypeScript·프로덕션 빌드를 확인했습니다.

- 목차의 여러 회차 선택과 저장한 묶음 작업 복원 화면을 연결했습니다. 계정별 checkpoint와
  원문 import·번역 요청 UUID 저장, 순차 처리·pause·cancel·lease·손상 기록 복구 테스트를 포함합니다.
- 리더의 글꼴·행간·여백·키·터치 방향, 목록의 선택적 스크롤, 본문 검색과 로컬 폴더·태그·즐겨찾기를
  연결했습니다. 모든 원문·번역·로컬 리더는 동일 계정의 독서 설정 provider를 사용합니다.
- 실제 서버가 반환할 수 있는 문단·본문·언어·날짜 범위와 완료 진행 건수·canRetry·페이지 총계를 검사해
  손상된 응답으로 잘못된 읽기나 페이지 이동을 제공하지 않게 했습니다.
- 업로드 metadata ID는 실제 서버와 같은 2,000자, 문단 ID는 200자이며 원문은 10,000문단·100만자입니다.
  계정 선호의 `und`·private-use-only 태그와 표시 이름의 제어문자를 서버와 동일하게 거부합니다.

새 화면의 한국어·영어 안내를 중앙 카탈로그에 포함했습니다. PDF 모듈의 기존 비차단 chunk 경고는 남아 있습니다.

## 카탈로그·읽기 기록 연결 경계 보강

2026-09-15 00:32:53 KST `npm run verify`에서 **132개 테스트·20개 파일 통과**,
번역·워크플로·계정·카탈로그 번역·JSON 카탈로그의 다섯 계약 생성 일치,
TypeScript 검사·프로덕션 빌드·공개 오프라인 자산 59개를 확인했습니다.

- 번역 작업 결과·서버 서재·기기 보관함의 문서 ID와 위치 helper를 통일했습니다.
  실제 메모와 번역 IndexedDB 저장소를 함께 사용해 기존 ID 메모 이전, 삭제 후 재생성 방지,
  오프라인 anchor 복구, 계정 격리와 localStorage 쓰기 차단 상태의 최신 위치 우선을 검증했습니다.
- PDF.js의 실제 텍스트 추출 실패를 주입해 이미지 페이지와 실패 상태를 구분하고,
  IndexedDB 왕복 후에도 전체 번역을 차단하면서 원본 페이지를 렌더링하는지 검사했습니다.
- 실제 TXT parse→번역 projection→업로드 입력의 조합과 PDF 이미지 placeholder 제외를 검사했습니다.
- JSON 카탈로그는 App 로그인 메모리 인증에 연결하고 웹소설 메뉴에서 파일 가져오기·읽기·번역으로
  진입합니다. 원격 주소·응답 한도·체크섬 검사와 카탈로그 제목/설명 번역의 별도 작업 계약을 포함합니다.
- 강조 범위·읽기 기록 내보내기/공유 회귀와 한국어·영어 메시지가 이번 빌드에 포함됐습니다.

이 결과는 자동 검증 범위입니다. 새 카탈로그의 실접속, 모바일 도구 배치와 브라우저의
파일 가져오기→번역→오프라인 재방문 흐름은 별도 종단 검증 대상입니다.

## 2026-09-15 ZIP 교환과 실제 브라우저 검증

- 웹 전체 163개 테스트·26개 파일, 계약 생성 검사·TypeScript·프로덕션 빌드 통과. 이후 이미지 전용 ZIP 통합 회귀 1개를 추가해 해당 4개 저장소 테스트 통과.
- 공통 Kotlin 테스트 17개 통과. 실제 Kotlin ZIP 출력 → 브라우저 codec 읽기/재출력 → Kotlin 읽기 검증.
- Android 전체 270개 테스트 통과, 선택적 live 14개 제외, debug APK 빌드 완료. 최종 codec 변경 뒤 portable 6개 테스트와 APK 재검증 완료.
- Android 실제 native mapper가 만든 EPUB/삽화/북마크/용어집 ZIP을 웹 UI에서 가져와 `Native source 🌏`와 `Read here` 북마크 확인.
- 웹 UI에서 로컬 원문·실제 Google 번역본·가져온 Android 책 3개를 ZIP으로 다운로드했다. 다운로드된 파일을 Android PortableLibraryStore와 PortableDocumentMapper에서 읽고, 각 문서·독서 기록·파일 bytes를 재내보내기로 대조하는 추가 테스트 1개 통과.
- 공통 ZIP의 PDF 원본을 브라우저 PDF.js canvas에서 실제 렌더링 확인.
- 390×844 모바일 ZIP 목록: document scrollWidth/scrollHeight가 390×844, 3개 행과 페이지 조작 표시, 보이는 모든 버튼 높이 44px 이상 확인.
- 같은 테스트 계정에서 앞서 TXT 파일 가져오기 → Google Web en→ko 번역 4/4 완료 → 한국어 리더 → 기기 저장을 확인했다. 실제 선택 `조용한 정원` 강조가 20px→24px 글자 재배치 후 유지됐고 `Tea page checkpoint` 북마크는 웹 ZIP→Android 검사에도 포함됐다.

Android 실기기 UI와 모든 외부 공급자·소설 사이트의 상시 가용성을 이 결과로 주장하지 않는다. 유료 공급자 API 키 없이 Google Web을 실제 검증 대상으로 사용했다.

2026-09-15 23:19 KST 대조 읽기 통합 후 웹 **175개 테스트·28개 파일**, 계약 검사·TypeScript·프로덕션 빌드가 통과했다. 실제 NovelBuddy 목록 조회 후 1페이지와 선행 수집한 2페이지가 저장 목록 화면에 표시되는 것을 확인했다.
