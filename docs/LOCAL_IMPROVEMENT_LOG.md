# 로컬 검증·개선 기록

## 2026-09-07 01:27 KST — 배포 후 정기 점검

배포 `2026-09-07T00:55:27+0900`의 웹·API·DB가 모두 정상이며 실제 브라우저 검사
2개가 3초에 통과했다. 로그인·서재·읽기 위치·책갈피·번역 백업 복원·로그아웃을 확인했다.
이번 실행에서는 Chrome 시작 시간 초과가 재발하지 않았으며, 현재 서버 프로세스의
시작 이후 로그에도 새로운 오류가 없었다. 추가 수정과 재배포 없이 현재 배포를 유지한다.
다음 후보는 세션 만료 후 미저장 읽기 위치의 복구 흐름이며, 이번 검사에는 포함하지 않았다.

## 2026-09-07 — 연속 페이지 이동 시 저장 충돌 안내 유지

관찰: `ProgressWriter`에서 첫 저장이 409 충돌로 실패하면 대기 중인 다음 저장은 일반
오류로 거절됐다. 화면은 409만 충돌로 분류하므로 연속 이동 시 충돌 안내가 네트워크
오류 안내로 덮일 수 있었다. 두 저장을 연속 호출하는 회귀 테스트에서 원인 유실을 재현했다.

반영: 중단 원인을 보관하고 대기 중인 저장과 이후 저장에도 같은 오류를 전달한다.
충돌 이후 서버에 추가 쓰기를 보내지 않는 동작은 유지한다.

검증: 수정 전 회귀 테스트 실패, 수정 후 프론트엔드 단위 테스트 17개·배포 관리 테스트
8개·데스크톱/모바일 브라우저 테스트 20개 통과. 서버 빌드·모듈 경계 검사와 프론트엔드
빌드/타입 검사도 통과했다. 배포 `2026-09-07T00:55:27+0900`에 반영 후 실제
Spring/PostgreSQL 연결 검증 2개 통과, 웹·서버·DB 정상.

환경 관찰: 최초 live 검사에서는 Chrome 시작이 시간 초과됐지만 재실행과 배포 후
검사는 모두 통과했다. 서버 로그에도 긴 시간 간격의 clock leap 경고가 있어 호스트
절전·일시 정지 영향 가능성을 기록한다. 앱 오류로 단정하거나 재시도로 실패를 숨기는
설정을 추가하지 않았다.

다음 후보: 세션 만료 후 재로그인 시 미저장 읽기 위치를 복구하는 흐름의 재현·검증.

## 2026-09-06 — Docker 중지 후 로컬 서비스 복구

관찰: 웹·API·DB가 모두 중지되어 있었고 `up`이 Docker daemon 연결 실패로 종료됐다.
Docker Desktop을 실행한 뒤 기존 DB 볼륨과 배포본으로 전체 스택을 복구했다.

반영:

- `up`과 `deploy`에 Docker 준비 검사를 추가했다. macOS에서 Docker가 중지되어 있으면
  Desktop을 실행하고 최대 90초 동안 준비를 기다린다.
- CLI 누락·시작 실패·준비 시간 초과 시 조치 방법을 안내한다. Docker 준비에 실패하면
  빌드나 현재 서비스 종료 전에 중단하여 기존 배포를 보존한다.
- 복구와 실패 경로를 검증하는 회귀 테스트 8개를 추가하고 이후 배포 검사에 포함했다.

검증: 관리 스크립트 테스트 8개와 실제 브라우저 테스트 2개 통과. 로그인·진행률·책갈피·
번역 백업 복원·새로고침·직접 로그인/로그아웃을 실제 Spring/PostgreSQL 연결로 확인했다.
웹·API·DB 모두 정상이다. 이번 변경은 실행 시 읽는 관리 스크립트에 적용되므로 앱 빌드
교체 없이 기존 배포 `2026-09-05T22:54:47+0900`을 유지했다. 기존 30분 개선 루프는 활성 상태다.

다음 후보: 서버 세션 만료와 네트워크 중단 후 읽기 및 저장 복구 흐름의 재현·검증.

## 2026-09-05 — 직접 사용하며 두 차례 개선 및 재배포

관찰: 빈 서재에서 독서 흐름을 바로 확인하기 어려웠고, 기본 번역 표시 설정 때문에
번역이 없는 책은 원문을 바로 읽기 어려웠다. 서버 서재의 로그인 입력도 설정 화면에
분리되어 있었다. 첫 배포를 브라우저에서 확인하니 원문 표시 중 선택 상자의 안내가
길고, 화면 분할 번호와 문서 페이지 번호가 같은 형태여서 구분하기 어려웠다.

반영:

- 빈 서재에 사용자가 선택해서 추가하는 한국어·영어 예제 책 버튼 추가.
- 번역이 없는 페이지는 원문을 바로 표시하고 표시 모드 선택도 실제 상태와 일치시킴.
- 서버 서재에서 계정과 비밀번호를 입력해 바로 로그인할 수 있도록 개선.
- 독서 화면 내부의 분할 번호에 `화면` / `View` 표시 추가.
- 배포 후보를 별도 포트 3110에서 회귀 검증한 뒤 실행 중인 배포본을 교체하도록 개선.
- 프론트엔드의 로컬 배포·검증 명령을 공용 전체 스택 관리 스크립트에 연결.

검증: 두 차례 배포 모두 단위 테스트 17개와 데스크톱·모바일 브라우저 회귀 테스트
20개를 통과했다. 최종 배포 `2026-09-05T22:54:47+0900`에서 실제 웹·Spring·PostgreSQL을
통과하는 백업 복원 및 서버 서재 직접 로그인·로그아웃 테스트 2개도 통과했다.
앱 내 브라우저에서 예제 책을 직접 열고 새로고침한 뒤 원문 선택 상태, 화면 번호,
본문과 조작 버튼의 배치를 확인했다. 웹·서버·DB 모두 정상이며 기존 30분 개선 루프를 유지한다.

## 2026-09-05 — 로컬 전체 스택 및 첫 검증 루프

관찰: 프론트엔드 미리보기만 실행되고 API·DB는 내려가 있어, 앞선 브라우저 테스트는
실제 저장 서버까지 통과하는 검증을 제공하지 못했다.

반영:

- 전용 PostgreSQL 볼륨, 로컬 인증 설정과 프론트엔드·서버의 실행 관리 명령 추가.
- Next standalone과 서버 JAR를 배포본별로 보관하여 다음 빌드와 현재 실행을 분리.
- 배포 실패 시 이전 애플리케이션 배포본을 재실행하고 DB 볼륨은 보존.
- 실제 브라우저 → Next 프록시 → Spring → PostgreSQL을 통과하는 live 검증 추가.
- 반복 검증은 같은 책·책갈피·번역을 재사용하도록 구성.

검증: 첫 실제 로그인·책 등록·진행률·책갈피·번역 백업 복원·새로고침 이어 읽기 통과.
웹·서버를 정상 종료 후 다시 실행한 두 번째 검증도 통과했다. PostgreSQL에서 테스트 책·책갈피·번역이 각각 1개임을 확인해 중복 누적이 없고 데이터가 유지됨을 검증했다. 로컬 3개 서비스는 모두 정상이다.

다음 후보(수정 전 관찰·재현 필요):

- 서버 연결 만료·네트워크 중단 후 UI에서 복구하는 흐름.
- 장문·큰 글꼴·작은 화면에서의 페이지 이동 및 앵커 유지.
- 큰 서재 목록과 번역 목록의 응답 크기·처리 시간.
